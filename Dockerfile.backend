# Backend Dockerfile
FROM node:22-alpine

# PDF generation is handled client-side (Canvas API) — no Chromium needed.

WORKDIR /app

COPY server/package*.json ./
RUN npm ci --only=production

COPY server/ ./

EXPOSE 5000
CMD ["node", "index.js"]
